# 三项目隔离重构实施计划

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 `ucas_earth/src` 下的三个功能模块（campus、mars、city）通过目录隔离实现完全独立，修改一个不影响其他。

**Architecture:** 保持 SPA 结构，将共享层（App.vue、router、main.ts）移入 `app/`，三个子项目各自包含独立的组件、类型、数据、API、工具函数。通过移动文件 + 更新 import 路径实现，不修改任何业务逻辑。

**Tech Stack:** Vue 3 + TypeScript + Vite + Cesium 1.138

---

## 文件映射

### app/（共享层）
| 操作 | 文件 | 说明 |
|------|------|------|
| 移动 | `App.vue` → `app/App.vue` | 删除 Cesium 导入 |
| 移动 | `main.ts` → `app/main.ts` | 更新 import 路径 |
| 移动 | `style.css` → `app/style.css` | 保持不变 |
| 移动 | `router/index.ts` → `app/router/index.ts` | 更新路由指向 |
| 移动 | `components/Menu.vue` → `app/components/Menu.vue` | 保持不变 |
| 移动 | `views/HomeView.vue` → `app/views/HomeView.vue` | 更新 import 路径 |

### campus/（校园展示）
| 操作 | 文件 | 说明 |
|------|------|------|
| 移动 | `views/CampusView.vue` → `campus/CampusView.vue` | 更新 import 路径 |
| 移动 | `components/Header.vue` → `campus/components/Header.vue` | 校园专用 |
| 移动 | `components/CesiumViewer.vue` → `campus/components/CesiumViewer.vue` | 校园专用 |
| 移动 | `components/SidebarContainer.vue` → `campus/components/` | |
| 移动 | `components/SidebarNav.vue` → `campus/components/` | |
| 移动 | `components/BuildingInfoPanel.vue` → `campus/components/` | |
| 移动 | `components/BuildingPopup.vue` → `campus/components/` | |
| 移动 | `components/CourseForm.vue` → `campus/components/` | |
| 移动 | `components/CourseImport.vue` → `campus/components/` | |
| 移动 | `components/CourseDetail.vue` → `campus/components/` | |
| 移动 | `components/CourseBubble.vue` → `campus/components/` | |
| 移动 | `components/EditPanel.vue` → `campus/components/` | |
| 移动 | `components/HoverTooltip.vue` → `campus/components/` | |
| 移动 | `components/LayerPanel.vue` → `campus/components/` | |
| 移动 | `components/PropertyTable.vue` → `campus/components/` | |
| 移动 | `components/ThemeToggle.vue` → `campus/components/` | |
| 移动 | `data/campus-buildings.ts` → `campus/data/` | |
| 移动 | `data/building-type-mapping.ts` → `campus/data/` | |
| 移动 | `api/geojson-db.ts` → `campus/api/` | |
| 移动 | `api/courses.ts` → `campus/api/` | |
| 移动 | `types/courses.ts` → `campus/types/` | |
| 移动 | `types/geojson-db.ts` → `campus/types/` | |
| 移动 | `utils/weather-effects.ts` → `campus/utils/` | |
| 移动 | `utils/weekRange.ts` → `campus/utils/` | |
| 移动 | `utils/__tests__/` → `campus/utils/__tests__/` | |

### mars/（火星探索）
| 操作 | 文件 | 说明 |
|------|------|------|
| 移动 | `views/MarsView.vue` → `mars/MarsView.vue` | 更新 import 路径 |
| 移动 | `components/Header.vue` → `mars/components/Header.vue` | 火星专用 |
| 移动 | `components/mars/MarsFlightPanel.vue` → `mars/components/MarsFlightPanel.vue` | |
| 移动 | `data/mars.ts` → `mars/data/` | |

### city/（城市漫游）
| 操作 | 文件 | 说明 |
|------|------|------|
| 移动 | `views/CityRoamingView.vue` → `city/CityRoamingView.vue` | 更新 import 路径 |
| 移动 | `components/Header.vue` → `city/components/Header.vue` | 城市专用 |

### shared/（共享类型）
| 操作 | 文件 | 说明 |
|------|------|------|
| 移动 | `types/building.ts` → `shared/types/` | campus 和 city 共享 |

---

## Task 1: 创建目录结构

**Files:**
- Create: `src/app/`、`src/campus/`、`src/mars/`、`src/city/`、`src/shared/`

- [ ] **Step 1: 创建所有子目录**

```bash
cd ucas_earth/src
mkdir -p app/views app/components app/router
mkdir -p campus/components campus/composables campus/types campus/data campus/api campus/utils campus/utils/__tests__
mkdir -p mars/components mars/types mars/data
mkdir -p city/components
mkdir -p shared/types
```

---

## Task 2: 移动 app 层文件

**Files:**
- Move: `App.vue` → `app/App.vue`
- Move: `main.ts` → `app/main.ts`
- Move: `style.css` → `app/style.css`
- Move: `router/index.ts` → `app/router/index.ts`
- Move: `components/Menu.vue` → `app/components/Menu.vue`
- Move: `views/HomeView.vue` → `app/views/HomeView.vue`

- [ ] **Step 1: 移动 App.vue 到 app/ 目录**

```bash
cd ucas_earth/src
mv App.vue app/App.vue
```

- [ ] **Step 2: 修改 App.vue — 删除 Cesium 导入**

```vue
<script setup lang="ts">
import Menu from "./components/Menu.vue";
</script>
```

- [ ] **Step 3: 移动 main.ts 到 app/ 目录**

```bash
mv main.ts app/main.ts
```

- [ ] **Step 4: 修改 main.ts — 更新 import 路径**

```typescript
import { createApp } from 'vue'
import App from './App.vue'
import router from './router'
import './style.css'

const app = createApp(App)
app.use(router)
app.mount('#app')
```

- [ ] **Step 5: 移动 style.css 到 app/ 目录**

```bash
mv style.css app/style.css
```

- [ ] **Step 6: 移动 router/index.ts 到 app/router/ 目录**

```bash
mv router/index.ts app/router/index.ts
```

- [ ] **Step 7: 修改 router/index.ts — 更新路由指向**

```typescript
import { createRouter, createWebHistory } from 'vue-router'

const routes = [
  {
    path: '/',
    name: 'home',
    component: () => import('../views/HomeView.vue'),
    meta: { title: '首页' },
  },
  {
    path: '/campus',
    name: 'campus',
    component: () => import('../../campus/CampusView.vue'),
    meta: { title: 'FYUN 校园系统', activePath: '/campus' },
  },
  {
    path: '/mars',
    name: 'mars',
    component: () => import('../../mars/MarsView.vue'),
    meta: { title: '火星探索', activePath: '/mars' },
  },
  {
    path: '/city',
    name: 'city',
    component: () => import('../../city/CityRoamingView.vue'),
    meta: { title: '城市漫游', activePath: '/city' },
  },
]

const router = createRouter({
  history: createWebHistory(),
  routes,
})

export default router
```

- [ ] **Step 8: 移动 Menu.vue 到 app/components/ 目录**

```bash
mv components/Menu.vue app/components/Menu.vue
```

- [ ] **Step 9: 移动 HomeView.vue 到 app/views/ 目录**

```bash
mv views/HomeView.vue app/views/HomeView.vue
```

- [ ] **Step 10: 验证 app 层 — 启动开发服务器确认无报错**

```bash
npm run dev
```

---

## Task 3: 移动 campus 层文件

**Files:**
- Move: `views/CampusView.vue` → `campus/CampusView.vue`
- Move: 15 个 components → `campus/components/`
- Move: `data/campus-buildings.ts`、`data/building-type-mapping.ts` → `campus/data/`
- Move: `api/geojson-db.ts`、`api/courses.ts` → `campus/api/`
- Move: `types/courses.ts`、`types/geojson-db.ts` → `campus/types/`
- Move: `utils/weather-effects.ts`、`utils/weekRange.ts` → `campus/utils/`
- Move: `utils/__tests__/` → `campus/utils/__tests__/`

- [ ] **Step 1: 移动 CampusView.vue**

```bash
mv views/CampusView.vue campus/CampusView.vue
```

- [ ] **Step 2: 移动所有 campus 组件**

```bash
mv components/Header.vue campus/components/Header.vue
mv components/CesiumViewer.vue campus/components/CesiumViewer.vue
mv components/SidebarContainer.vue campus/components/
mv components/SidebarNav.vue campus/components/
mv components/BuildingInfoPanel.vue campus/components/
mv components/BuildingPopup.vue campus/components/
mv components/CourseForm.vue campus/components/
mv components/CourseImport.vue campus/components/
mv components/CourseDetail.vue campus/components/
mv components/CourseBubble.vue campus/components/
mv components/EditPanel.vue campus/components/
mv components/HoverTooltip.vue campus/components/
mv components/LayerPanel.vue campus/components/
mv components/PropertyTable.vue campus/components/
mv components/ThemeToggle.vue campus/components/
```

- [ ] **Step 3: 移动 campus 数据文件**

```bash
mv data/campus-buildings.ts campus/data/
mv data/building-type-mapping.ts campus/data/
```

- [ ] **Step 4: 移动 campus API 文件**

```bash
mv api/geojson-db.ts campus/api/
mv api/courses.ts campus/api/
```

- [ ] **Step 5: 移动 campus 类型文件**

```bash
mv types/courses.ts campus/types/
mv types/geojson-db.ts campus/types/
```

- [ ] **Step 6: 移动 campus 工具文件**

```bash
mv utils/weather-effects.ts campus/utils/
mv utils/weekRange.ts campus/utils/
mv utils/__tests__/ campus/utils/__tests__/
```

- [ ] **Step 7: 修改 CampusView.vue — 更新所有 import 路径**

将 `../components/xxx` 改为 `./components/xxx`，`../api/xxx` 改为 `./api/xxx`，以此类推。

- [ ] **Step 8: 修改 campus 内部组件的 import 路径**

更新 `CesiumViewer.vue`、`Header.vue` 等组件中引用 `../data/`、`../types/`、`../utils/` 的路径。

- [ ] **Step 9: 验证 campus 视图 — 访问 /campus 确认正常**

```bash
npm run dev
# 访问 http://localhost:5173/campus
```

---

## Task 4: 移动 mars 层文件

**Files:**
- Move: `views/MarsView.vue` → `mars/MarsView.vue`
- Move: `components/Header.vue` → `mars/components/Header.vue`
- Move: `components/mars/MarsFlightPanel.vue` → `mars/components/MarsFlightPanel.vue`
- Move: `data/mars.ts` → `mars/data/`

- [ ] **Step 1: 移动 MarsView.vue**

```bash
mv views/MarsView.vue mars/MarsView.vue
```

- [ ] **Step 2: 移动 mars 组件**

```bash
mv components/mars/MarsFlightPanel.vue mars/components/
mv components/Header.vue mars/components/Header.vue
```

- [ ] **Step 3: 移动 mars 数据**

```bash
mv data/mars.ts mars/data/
```

- [ ] **Step 4: 修改 MarsView.vue — 更新 import 路径**

将 `../components/Header.vue` 改为 `./components/Header.vue`，`../components/mars/MarsFlightPanel.vue` 改为 `./components/MarsFlightPanel.vue`，`../data/mars.ts` 改为 `./data/mars.ts`。

- [ ] **Step 5: 验证 mars 视图 — 访问 /mars 确认正常**

```bash
npm run dev
# 访问 http://localhost:5173/mars
```

---

## Task 5: 移动 city 层文件

**Files:**
- Move: `views/CityRoamingView.vue` → `city/CityRoamingView.vue`
- Move: `components/Header.vue` → `city/components/Header.vue`

- [ ] **Step 1: 移动 CityRoamingView.vue**

```bash
mv views/CityRoamingView.vue city/CityRoamingView.vue
```

- [ ] **Step 2: 移动 city 组件**

```bash
mv components/Header.vue city/components/Header.vue
```

- [ ] **Step 3: 修改 CityRoamingView.vue — 更新 import 路径**

将 `../components/Header.vue` 改为 `./components/Header.vue`。

- [ ] **Step 4: 验证 city 视图 — 访问 /city 确认正常**

```bash
npm run dev
# 访问 http://localhost:5173/city
```

---

## Task 6: 移动 shared 层文件

**Files:**
- Move: `types/building.ts` → `shared/types/building.ts`

- [ ] **Step 1: 移动共享类型**

```bash
mv types/building.ts shared/types/
```

- [ ] **Step 2: 更新引用 building.ts 的组件 import 路径**

检查 campus 和 city 中引用 `../types/building` 的文件，更新为 `../../shared/types/building`。

- [ ] **Step 3: 验证所有视图 — 逐个访问确认正常**

```bash
npm run dev
# 访问 http://localhost:5173/ （首页）
# 访问 http://localhost:5173/campus （校园展示）
# 访问 http://localhost:5173/mars （火星探索）
# 访问 http://localhost:5173/city （城市漫游）
```

---

## Task 7: 清理与最终验证

- [ ] **Step 1: 删除空目录**

```bash
cd ucas_earth/src
# 删除已清空的旧目录
rm -rf views components/mars data api types utils
```

- [ ] **Step 2: 运行构建验证**

```bash
npm run build
```

- [ ] **Step 3: 运行测试验证**

```bash
npm test
```

- [ ] **Step 4: 最终检查 — 确认四个页面全部正常**

1. 首页 `/` — 正常显示
2. 校园展示 `/campus` — 搜索、图层切换、建筑信息正常
3. 火星探索 `/mars` — 火星车、地标、飞行面板正常
4. 城市漫游 `/city` — 城市搜索、飞行定位正常
