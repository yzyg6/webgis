# 空间分析页面实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增「空间分析」功能模块，包含可视域分析的页面布局、交互框架和预留接口，核心分析代码由用户后续提供。

**Architecture:** 独立 `src/analysis/` 模块，参照 `src/city/` 的目录结构。AnalysisView 管理 Cesium.Viewer 生命周期和分析状态，ViewshedPanel 侧边面板负责参数输入和操作触发，Header 提供底图切换。核心 viewshed 分析逻辑预留接口，待用户后续替换。

**Tech Stack:** Vue 3 (`<script setup>`) + TypeScript + Cesium 1.141 + Element Plus + vue-router

**Spec:** `docs/superpowers/specs/2026-05-31-空间分析页面设计.md`

---

## 文件结构

| 操作 | 文件路径 | 职责 |
|------|---------|------|
| Create | `src/analysis/types/analysis.ts` | 类型定义 |
| Create | `src/analysis/composables/useTheme.ts` | 主题 composable |
| Create | `src/analysis/components/ThemeToggle.vue` | 主题切换按钮 |
| Create | `src/analysis/components/Header.vue` | 顶栏（标题 + 底图切换） |
| Create | `src/analysis/components/ViewshedPanel.vue` | 可视域参数面板 |
| Create | `src/analysis/AnalysisView.vue` | 主视图（Cesium + 面板 + 状态协调） |
| Modify | `src/app/router/index.ts:15-20` | 新增 `/analysis` 路由 |
| Modify | `src/app/components/Menu.vue:9` | 新增「空间分析」菜单项 |

---

### Task 1: 类型定义

**Files:**
- Create: `ucas_earth/src/analysis/types/analysis.ts`

- [ ] **Step 1: 创建类型定义文件**

```ts
/** 可视域分析参数 */
export interface ViewshedParams {
  /** 观察者高度偏移（m） */
  observerHeight: number
  /** 目标高度偏移（m） */
  targetHeight: number
  /** 分析半径（m） */
  radius: number
}

/** 分析状态 */
export type AnalysisStatus = 'idle' | 'picking' | 'picked' | 'analyzing' | 'done' | 'cleared'

/** 底图类型 */
export type BaseLayerType = 'osm' | 'arcgis' | 'carto' | 'google-satellite'
```

- [ ] **Step 2: 验证类型文件无语法错误**

Run: `cd ucas_earth && npx tsc --noEmit src/analysis/types/analysis.ts`
Expected: 无错误输出

- [ ] **Step 3: Commit**

```bash
git add src/analysis/types/analysis.ts
git commit -m "feat(analysis): 添加空间分析类型定义"
```

---

### Task 2: 主题 Composable

**Files:**
- Create: `ucas_earth/src/analysis/composables/useTheme.ts`

参照 `src/mars/composables/useTheme.ts` 实现。

- [ ] **Step 1: 读取现有的 useTheme 实现**

Read: `src/mars/composables/useTheme.ts`

- [ ] **Step 2: 创建 useTheme composable**

复制 `src/mars/composables/useTheme.ts` 的内容到 `src/analysis/composables/useTheme.ts`，保持一致。

- [ ] **Step 3: Commit**

```bash
git add src/analysis/composables/useTheme.ts
git commit -m "feat(analysis): 添加主题 composable"
```

---

### Task 3: ThemeToggle 组件

**Files:**
- Create: `ucas_earth/src/analysis/components/ThemeToggle.vue`

参照 `src/city/components/ThemeToggle.vue` 实现。

- [ ] **Step 1: 读取现有的 ThemeToggle 实现**

Read: `src/city/components/ThemeToggle.vue`

- [ ] **Step 2: 创建 ThemeToggle 组件**

复制 `src/city/components/ThemeToggle.vue` 的内容到 `src/analysis/components/ThemeToggle.vue`，保持一致。

- [ ] **Step 3: Commit**

```bash
git add src/analysis/components/ThemeToggle.vue
git commit -m "feat(analysis): 添加主题切换组件"
```

---

### Task 4: Header 组件

**Files:**
- Create: `ucas_earth/src/analysis/components/Header.vue`

参照 `src/city/components/Header.vue` 的城市漫游模式简化实现：仅保留标题和底图切换菜单。

- [ ] **Step 1: 读取现有 Header 完整实现**

Read: `src/city/components/Header.vue`（完整文件）

- [ ] **Step 2: 创建 Header 组件**

实现要点：
- Props: `mode: string`（固定 `'analysis'`）
- Emits: `switch-layer`（底图切换）
- 左侧：标题「空间分析」+ 副标题「Spatial Analysis」
- 右侧：底图切换菜单项（OSM / ArcGIS / Carto Light / Google Satellite）
- 使用 `mode === 'analysis'` 条件渲染对应菜单
- 样式完全复用 `Header.vue` 的 CSS class（`.app-header`, `.title-wrap`, `.menu-bar`, `.menu-item`, `.submenu` 等）

- [ ] **Step 3: 验证组件无语法错误**

Run: `cd ucas_earth && npx vue-tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 4: Commit**

```bash
git add src/analysis/components/Header.vue
git commit -m "feat(analysis): 添加空间分析 Header 组件"
```

---

### Task 5: ViewshedPanel 参数面板

**Files:**
- Create: `ucas_earth/src/analysis/components/ViewshedPanel.vue`

参照 `src/city/components/CityPanel.vue` 的可折叠侧边面板模式。

- [ ] **Step 1: 创建 ViewshedPanel 组件骨架**

实现要点：
- Props: `isPicking`, `isAnalyzing`, `hasResult`（均为 boolean），`observerHeight`, `targetHeight`, `radius`（均为 number）
- Emits: `update:observerHeight`, `update:targetHeight`, `update:radius`, `start-pick`, `start-analyze`, `clear-result`
- 可折叠状态：`isCollapsed` ref

- [ ] **Step 2: 实现面板模板**

模板结构：
1. **标题区**：`⊕ 可视域分析` + 折叠按钮（`‹` / `›`）
2. **参数区**（3 个输入组）：
   - 观察者高度 (m)：`<input type="number" :value="observerHeight" min="0.5" max="50" step="0.5">`
   - 目标高度 (m)：`<input type="number" :value="targetHeight" min="0.5" max="100" step="0.5">`
   - 分析半径 (m)：`<input type="number" :value="radius" min="100" max="5000" step="50">`
3. **操作区**（3 个按钮）：
   - 「📍 选取观察点」：disabled when `isPicking || isAnalyzing`，高亮 when `isPicking`
   - 「▶ 开始分析」：disabled when `!hasResult && !isPicking`，即只有选取了观察点后才能分析
   - 「✕ 清除结果」：disabled when `!hasResult`
4. **状态区**：根据 `status` computed 显示状态文字

- [ ] **Step 3: 实现样式**

样式参照 `CityPanel.vue`：
- `.viewshed-panel` 宽度 240px，暗色背景，支持 `.collapsed` 收缩到 40px
- `.panel-header` 标题区，flex 布局
- `.panel-body` 参数区，padding + overflow-y: auto
- 输入组 `.param-group`：label + input，暗色边框
- 按钮 `.action-btn`：全宽，圆角，语义色（蓝=选取，绿=分析，红=清除）
- 状态文字 `.status-text`：底部居中，小字

- [ ] **Step 4: 验证组件无语法错误**

Run: `cd ucas_earth && npx vue-tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 5: Commit**

```bash
git add src/analysis/components/ViewshedPanel.vue
git commit -m "feat(analysis): 添加可视域分析参数面板"
```

---

### Task 6: AnalysisView 主视图

**Files:**
- Create: `ucas_earth/src/analysis/AnalysisView.vue`

参照 `src/city/CityRoamingView.vue` 的结构。

- [ ] **Step 1: 创建 AnalysisView 骨架**

实现要点：
- 导入：Header, ViewshedPanel, ThemeToggle, useTheme, * as Cesium
- Refs: `cesiumContainer`, `isPicking`, `isAnalyzing`, `hasResult`, `observerHeight`(2), `targetHeight`(2), `radius`(500), `currentBaseLayer`
- 变量: `viewer: Cesium.Viewer | null = null`, `handler: Cesium.ScreenSpaceEventHandler | null = null`

- [ ] **Step 2: 实现 onMounted（Cesium 初始化）**

参照 `CityRoamingView.vue` 的 `onMounted`：
1. `Cesium.Ion.defaultAccessToken = import.meta.env.VITE_CESIUM_TOKEN`
2. 创建 `Cesium.Viewer`（同 CityRoamingView 的 options）
3. 加载 OSM 底图（`Cesium.OpenStreetMapImageryProvider`）
4. 飞到默认位置（阜阳：lon 115.778, lat 32.886, height 5000000）

- [ ] **Step 3: 实现地形加载**

在 `onMounted` 中 Viewer 创建后添加：
```ts
try {
  viewer.terrainProvider = await Cesium.CesiumTerrainProvider.fromUrl(
    Cesium.IonResource.fromAssetId(1),
    { requestWaterMask: true }
  )
} catch (e) {
  console.warn('地形加载失败，使用默认椭球体:', e)
}
viewer.scene.globe.depthTestAgainstTerrain = true
```

- [ ] **Step 4: 实现地图点击交互**

创建 `ScreenSpaceEventHandler`：
```ts
handler = new Cesium.ScreenSpaceEventHandler(viewer.scene.canvas)
handler.setInputAction((movement: Cesium.ScreenSpaceEventHandler.PositionedEvent) => {
  if (!isPicking.value || !viewer) return
  const ray = viewer.camera.getPickRay(movement.position)
  if (!ray) return
  const position = viewer.scene.globe.pick(ray, viewer.scene)
  if (!position) return
  isPicking.value = false
  hasResult.value = true
  // === 预留：调用 viewshed 分析 ===
  // handleAnalyse(position)
}, Cesium.ScreenSpaceEventType.LEFT_CLICK)
```

- [ ] **Step 5: 实现底图切换**

参照 `CityRoamingView.vue` 的底图切换逻辑，实现 `switchBaseLayer` 函数：
- 清除现有 imageryLayers
- 根据 `layerType` 添加对应底图（osm / arcgis / carto / google-satellite）

- [ ] **Step 6: 实现操作处理函数**

```ts
const handleStartPick = () => {
  isPicking.value = true
  hasResult.value = false
}

const handleStartAnalyse = () => {
  // === 预留：用户后续提供核心分析逻辑 ===
  // isAnalyzing.value = true
  // viewshedInstance.analyse(...)
  // isAnalyzing.value = false
  // hasResult.value = true
  console.log('视域分析 - 预留接口：等待核心代码')
}

const handleClearResult = () => {
  // === 预留：用户后续提供清除逻辑 ===
  // viewshedInstance?.clear()
  hasResult.value = false
  isPicking.value = false
}
```

- [ ] **Step 7: 实现 onUnmounted**

```ts
onUnmounted(() => {
  handler?.destroy()
  if (viewer && !viewer.isDestroyed()) {
    viewer.destroy()
    viewer = null
  }
})
```

- [ ] **Step 8: 实现模板**

模板结构（参照 CityRoamingView）：
```html
<div class="analysis-view">
  <Header mode="analysis" @switch-layer="switchBaseLayer" />
  <div class="analysis-content">
    <ViewshedPanel
      :is-picking="isPicking"
      :is-analyzing="isAnalyzing"
      :has-result="hasResult"
      :observer-height="observerHeight"
      :target-height="targetHeight"
      :radius="radius"
      @update:observer-height="observerHeight = $event"
      @update:target-height="targetHeight = $event"
      @update:radius="radius = $event"
      @start-pick="handleStartPick"
      @start-analyse="handleStartAnalyse"
      @clear-result="handleClearResult"
    />
    <div class="cesium-container" ref="cesiumContainer"></div>
  </div>
</div>
```

- [ ] **Step 9: 实现样式**

```css
.analysis-view {
  width: 100%;
  height: 100vh;
  display: flex;
  flex-direction: column;
  background: var(--bg-app);
}

.analysis-content {
  flex: 1;
  display: flex;
  overflow: hidden;
}

.cesium-container {
  flex: 1;
  min-width: 0;
  height: 100%;
}
```

- [ ] **Step 10: 验证组件无语法错误**

Run: `cd ucas_earth && npx vue-tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 11: Commit**

```bash
git add src/analysis/AnalysisView.vue
git commit -m "feat(analysis): 添加空间分析主视图"
```

---

### Task 7: 路由配置

**Files:**
- Modify: `ucas_earth/src/app/router/index.ts`

- [ ] **Step 1: 新增路由条目**

在 `routes` 数组中（第 4 个路由，`/city` 之后）添加：

```ts
{
  path: '/analysis',
  name: 'analysis',
  component: () => import('../../analysis/AnalysisView.vue'),
  meta: { title: '空间分析', activePath: '/analysis' },
},
```

- [ ] **Step 2: 验证路由配置无错误**

Run: `cd ucas_earth && npx vue-tsc --noEmit`
Expected: 无新增错误

- [ ] **Step 3: Commit**

```bash
git add src/app/router/index.ts
git commit -m "feat(analysis): 添加空间分析路由配置"
```

---

### Task 8: 菜单配置

**Files:**
- Modify: `ucas_earth/src/app/components/Menu.vue`

- [ ] **Step 1: 新增菜单项**

在 `menuItems` 数组中添加第四项：

```ts
{ index: '/analysis', title: '空间分析', icon: '⊕' },
```

- [ ] **Step 2: 验证菜单渲染**

Run: `cd ucas_earth && npm run dev`
在浏览器中访问 `http://localhost:5173`，确认：
- 左侧菜单显示四个项：首页、校园展示、火星探索、城市漫游、空间分析
- 点击「空间分析」导航到 `/analysis`

Expected: 菜单正确显示，点击可导航

- [ ] **Step 3: Commit**

```bash
git add src/app/components/Menu.vue
git commit -m "feat(analysis): 添加空间分析菜单入口"
```

---

### Task 9: 端到端验证

- [ ] **Step 1: 启动开发服务器**

Run: `cd ucas_earth && npm run dev`

- [ ] **Step 2: 验证完整流程**

在浏览器中访问 `http://localhost:5173/analysis`：
1. 页面正确加载，Header 显示「空间分析」
2. 左侧 ViewshedPanel 显示参数面板（观察者高度、目标高度、分析半径）
3. Cesium 3D 地球正常渲染，加载 OSM 底图
4. 面板折叠/展开正常工作
5. 点击「选取观察点」按钮，状态变为「请在地图上点击选取观察点」
6. 点击地图，状态变为「观察点已设置，点击开始分析」
7. 点击「开始分析」，控制台输出预留接口提示
8. 点击「清除结果」，状态重置为 idle
9. 底图切换菜单可正常切换底图

Expected: 所有交互流程正常，无控制台错误

- [ ] **Step 3: 验证类型检查**

Run: `cd ucas_earth && npx vue-tsc --noEmit`
Expected: 无错误

- [ ] **Step 4: 最终 Commit**

```bash
git add -A
git commit -m "feat(analysis): 完成空间分析页面布局和交互框架"
```
